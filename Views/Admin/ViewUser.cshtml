@model List<WebApplication1.Models.Domain.ApplicationUser>

<h1>Users</h1>


<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Email</th>
            <th>Username</th>
            <th>Manupulation</th>
        </tr>
    </thead>
    <tbody>
        @foreach(var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Email</td>
                <td>@item.UserName</td>
                <td><a href="Admin/View/@item.Id">View</a></td>
                
                
            </tr>
        }
    </tbody>
</table>